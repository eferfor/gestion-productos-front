export interface ProductApi {
  id: number;
  nombre: string;
  descripcion: string;
  precio: number;
  categoria: string;
  marca: string;
  referencia: string;
  activo: boolean;
  fecha_alta: string;
  aud_user: string;
}
